SELECT Salary
FROM Employee
ORDER BY DESC
Salary
-- Ordenando o salário na ordem descrescente
-- O maior salário vai aparecer primeiro 
DESC 
-- Vai começar a mostrar do segundo salário ("1,..."), antes da virgula
-- ("...,1") E somente um único valor  depois da virgula
LIMIT 1,1